{
  "name": "amp-plugins",
  "private": true,
  "description": "A collection of focused plugins for Amp.",
  "license": "MIT",
  "packageManager": "bun@1.4.0",
  "workspaces": [
    "plugins/*"
  ],
  "scripts": {
    "build": "bun run --filter '@nguyenphutrong/*' build",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "check": "bun run test && bun run typecheck && bun run build && git diff --exit-code -- 'plugins/*/dist'"
  },
  "devDependencies": {
    "@ampcode/plugin": "0.0.0-20260825001040-g9dff10c",
    "@types/bun": "1.4.0",
    "typescript": "7.0.2"
  }
}
