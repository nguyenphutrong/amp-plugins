{
  "name": "@nguyenphutrong/amp-markdown-commands",
  "version": "0.1.0",
  "private": true,
  "description": "Register reusable Amp command-palette prompts from Markdown files.",
  "license": "MIT",
  "scripts": {
    "build": "bun build src/index.ts --target=bun --format=esm --external @ampcode/plugin --outfile dist/index.ts",
    "test": "bun test test"
  }
}
